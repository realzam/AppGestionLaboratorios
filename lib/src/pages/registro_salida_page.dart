import 'package:flutter/material.dart';

class RegistrarSalidaPage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Page Registrar salida'),
    );
  }
}